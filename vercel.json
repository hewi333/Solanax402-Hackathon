{
  "buildCommand": "cd learnearn && npm install && npm run build",
  "outputDirectory": "learnearn/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

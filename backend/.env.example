# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com

# Treasury Wallet Configuration (keep this SECRET!)
#
# RECOMMENDED: Base58-encoded private key (easiest method)
# Generate with: solana-keygen new --outfile treasury-keypair.json
# Then get the base58 key: solana-keygen pubkey --keypair treasury-keypair.json
# To export base58 private key, use: cat treasury-keypair.json | jq -r '.' | python3 -c "import sys, json, base58; print(base58.b58encode(bytes(json.load(sys.stdin))).decode())"
# Or use online tools to convert the JSON array to base58
# Format: TREASURY_WALLET_KEYPAIR=your_base58_private_key_here
#
# ALTERNATIVE: JSON array format (legacy support)
# Copy the entire array of numbers from treasury-keypair.json
# Format: TREASURY_WALLET_KEYPAIR=[1,2,3,4,5...]
#
TREASURY_WALLET_KEYPAIR=

# =====================================================
# Coinbase CDP (Developer Platform) Configuration
# =====================================================
# Enable embedded wallet features with Coinbase CDP
# Get your API credentials from: https://portal.cdp.coinbase.com/
#
# Steps to get CDP credentials:
# 1. Go to https://portal.cdp.coinbase.com/
# 2. Create a new project or select existing
# 3. Navigate to "API Keys" section
# 4. Create a new API key
# 5. Copy the API Key Name and Private Key
# 6. Add them to Railway/Vercel environment variables
#
CDP_API_KEY_NAME=your_cdp_api_key_name_here
CDP_API_KEY_PRIVATE_KEY=your_cdp_private_key_here

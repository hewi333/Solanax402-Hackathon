# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com

# Treasury Wallet Configuration (keep this SECRET!)
#
# RECOMMENDED: Base58-encoded private key (easiest method)
# Generate with: solana-keygen new --outfile treasury-keypair.json
# Then get the base58 key: solana-keygen pubkey --keypair treasury-keypair.json
# To export base58 private key, use: cat treasury-keypair.json | jq -r '.' | python3 -c "import sys, json, base58; print(base58.b58encode(bytes(json.load(sys.stdin))).decode())"
# Or use online tools to convert the JSON array to base58
# Format: TREASURY_WALLET_KEYPAIR=your_base58_private_key_here
#
# ALTERNATIVE: JSON array format (legacy support)
# Copy the entire array of numbers from treasury-keypair.json
# Format: TREASURY_WALLET_KEYPAIR=[1,2,3,4,5...]
#
TREASURY_WALLET_KEYPAIR=

# =====================================================
# Coinbase CDP (Developer Platform) v2 Configuration
# =====================================================
# Enable embedded wallet features with Coinbase CDP Server Wallets v2
# Get your API credentials from: https://portal.cdp.coinbase.com/
#
# Steps to get CDP v2 credentials:
# 1. Go to https://portal.cdp.coinbase.com/
# 2. Create a new project or select existing project
# 3. Navigate to "API Keys" section
# 4. Click "Create API Key"
# 5. You'll receive TWO credentials (minimum):
#    - API Key ID (simple string, no path needed) - REQUIRED
#    - API Key Secret - REQUIRED
#    - Wallet Secret (optional - only if CDP wallets need to sign transactions)
# 6. Add the required credentials to Railway environment variables
#
# NOTE: With CDP v2, you DON'T need the "organizations/..." format!
# Just use the simple API Key ID and secret directly.
#
# ARCHITECTURE:
# - Your Treasury Wallet (TREASURY_WALLET_KEYPAIR) sends rewards TO users
# - CDP wallets are created FOR users to RECEIVE rewards
# - CDP_WALLET_SECRET only needed if users need to SEND from CDP wallets
#
# Example values (not real):
# CDP_API_KEY_ID=abc123def456
# CDP_API_KEY_SECRET=secret_xyz789
#
CDP_API_KEY_ID=your_api_key_id_here
CDP_API_KEY_SECRET=your_api_key_secret_here

# OPTIONAL - Only needed if CDP wallets need to sign/send transactions
# For receiving-only wallets, you can leave this blank
CDP_WALLET_SECRET=
